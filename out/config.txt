# config for the 32 bit kernel + OS

# sets gpu mem allocation in MB
gpu_mem=64

# file name for the kernel we are using
kernel=kernel7.img

# prevent overwriting code
disable_commandline_tags=1

# start kernel at address 0x0
kernel_old=1


